const express = require("express");
const router = express.Router();
const Cocktail = require("../models/Cocktail");

// define CRUD operations (Create, Read, Update, Delete) on cocktails.

module.export = router;
